const User = require('./User');
const Thought = require('./Thought');

module.exports = { User, Thought };

//Take the models from the User and Thought files and export them out to be used by the rest of the files
